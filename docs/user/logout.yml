paths:
  /auth/logout/:                # path of the user from your endpoint
    post:                 # endpoint request type (post request)
      tags:               # Tag property
        - User            # Value of the tag
      summary: signs out a registered user
      requestBody:
        required: false
      responses:          # server responses
        204:
          description: An empty response, Json Web Token in cookie is set to null